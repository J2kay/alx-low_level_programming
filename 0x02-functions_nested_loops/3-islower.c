#include "main.h"
/**
 * _islower - checks if the char is lowercase.
 * @c: The character to print
 * Return: 1 on success else return 0
 */

int _islower(int c)
{


	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
{
	return (0);
}
}
